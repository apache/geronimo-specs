o the directory names need to match the artifactId, right now
  the directory names are foo but the artifact ids are geronimo-spec-foo

o javamail resources need to be adjusted

o the jsp resources need to be adjusted

o the servlet resources need to be adjusted

o an assembly needs to be made for the combined ejb artifact
